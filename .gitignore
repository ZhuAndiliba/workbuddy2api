# Auth & secrets
auths/
data/
*.key
*.pem

# Binaries (anchored to repo root only — bare "login"/"credit" would also ignore cmd/login/, cmd/credit/)
/login
/credit
/wb2api
/signin_bin
/console

# Config with secrets (use config.example.json)
# config.json 含 api_key 与控制台 token；旧的单实例配置名一并排除，防止误提交。
config.json
config.cn.json
config.global.json
!config.example.json

# Build artifacts
*.tmp
__pycache__/

# Backups (contains auth tokens)
backups/

# IDE
.idea/
.vscode/
LOOP.md
PLAN.md
SPEC.md
docs/

# 设计/计划文档（但 README.md 必须进版本库）
*.md
!README.md

# GUI 测试证据（截图）
gui-test-screenshots/
